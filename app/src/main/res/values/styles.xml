<resources>

    <!-- Estilo base de la aplicación. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Estilo de la pantalla Splash -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <!-- Estilo sin la Actionbar -->
    <style name="AtNoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Campo de texto-->
    <style name="EditText" parent="AppTheme">
        <!-- cursor y línea normal-->
        <item name="colorControlNormal">@color/colorPrimaryDark</item>
        <!-- cursor y línea con foco-->
        <item name="colorControlActivated">@color/colorPrimary</item>
    </style>

    <!-- Etiqueta flotante -->
    <style name="AppTheme.FloatingLabel" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/purple</item>
        <item name="android:textSize">@dimen/fab_margin</item>
    </style>
    <style name="AppTheme.FloatingLabelEror" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/red</item>
        <item name="android:textSize">@dimen/fab_margin</item>
    </style>

    <!-- Estilos para txt -->
    <style name="AppTheme.TextFloatingLabel" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">20sp</item>
    </style>
    <style name="AppTheme.TextBox" parent="@android:style/TextAppearance">
        <item name="android:ems">10</item>
        <item name="android:textSize">22dp</item>
        <item name="android:background">@color/transparent</item>
        <item name="android:drawablePadding">12dp</item>
        <item name="android:padding">8dp</item>
        <item name="android:maxLines">1</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>

</resources>
